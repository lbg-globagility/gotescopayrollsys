
# INSERT INTO `view` (`ViewName`, `OrganizationID`)

SELECT
l.DisplayValue
,og.RowID
FROM listofval l
INNER JOIN organization og ON og.RowID IS NOT NULL
WHERE l.`Type`='Report List'
AND l.`Active`='Yes'
ORDER BY og.RowID, l.DisplayValue;
