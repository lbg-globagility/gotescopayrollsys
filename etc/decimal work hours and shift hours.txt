ALTER TABLE `shift`
	CHANGE COLUMN `ShiftHours` `ShiftHours` DECIMAL(10,2) NULL DEFAULT 0 AFTER `DivisorToDailyRate`,
	CHANGE COLUMN `WorkHours` `WorkHours` DECIMAL(10,2) NULL DEFAULT 0 AFTER `ShiftHours`;
	
	
UPDATE shift sh SET sh.LastUpd=IFNULL(ADDDATE(sh.LastUpd, INTERVAL 1 SECOND), CURRENT_TIMESTAMP());

