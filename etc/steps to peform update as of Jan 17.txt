- update the database function INSUPD_position
- execute the result of this query :
SELECT
CONCAT_WS(', ', CONCAT('SELECT INSUPD_position(', pos.RowID)
,CONCAT('\'', pos.PositionName, '\'')
,2
,pos.OrganizationID
,2
,'NULL'
,CONCAT(pos.DivisionId, ');')
) `Result`
FROM (SELECT *
		FROM `position`
		WHERE LENGTH(PositionName) > 0
		#GROUP BY PositionName
		) pos
ORDER BY pos.PositionName
;

