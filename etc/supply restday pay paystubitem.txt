# INSERT INTO `paystubitem` (`OrganizationID`, `Created`, `CreatedBy`, `LastUpd`, `LastUpdBy`, `PayStubID`, `ProductID`, `PayAmount`, `Undeclared`)

SELECT p.OrganizationID
,CURRENT_TIMESTAMP()
,1
,CURRENT_TIMESTAMP()
,1
,ps.RowID
,p.RowID
,0
,FALSE # FALSE TRUE
FROM product p
INNER JOIN paystub ps ON ps.RowID IS NOT NULL AND ps.OrganizationID=p.OrganizationID
WHERE p.PartNo='Restday pay'
;

